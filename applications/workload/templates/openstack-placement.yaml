---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.openstack_placement.name }}
spec:
  syncPolicy:
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true
  destination:
    namespace: {{ .Values.openstack_keystone.namespace }}
    server: https://192.168.56.151:6443
  project: default
  source:
    chart: placement
    repoURL: https://tarballs.opendev.org/openstack/openstack-helm
    targetRevision: {{ .Values.openstack_placement.version }}
    helm:
      valueFiles:
        - values.yaml
      values: |-
        {{- toYaml .Values.openstack_placement.values | nindent 8 }}
