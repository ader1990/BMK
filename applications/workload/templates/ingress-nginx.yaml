---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.ingress_nginx.name }}
spec:
  syncPolicy:
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true
  destination:
    namespace: {{ .Values.ingress_nginx.namespace }}
    server: https://192.168.56.151:6443
  project: default
  source:
    chart: ingress-nginx
    repoURL: https://kubernetes.github.io/ingress-nginx
    targetRevision: {{ .Values.ingress_nginx.version }}
    helm:
      valueFiles:
        - values.yaml
      values: |-
        {{- toYaml .Values.ingress_nginx.values | nindent 8 }}
