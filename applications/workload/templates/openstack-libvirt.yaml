---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.openstack_libvirt.name }}
spec:
  syncPolicy:
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true
  destination:
    namespace: {{ .Values.openstack_libvirt.namespace }}
    server: https://192.168.56.151:6443
  project: default
  source:
    chart: libvirt
    repoURL: https://tarballs.opendev.org/openstack/openstack-helm
    targetRevision: {{ .Values.openstack_libvirt.version }}
    helm:
      valueFiles:
        - values.yaml
      values: |-
        {{- toYaml .Values.openstack_libvirt.values | nindent 8 }}
