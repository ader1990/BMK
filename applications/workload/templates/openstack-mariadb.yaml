---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.openstack_mariadb.name }}
spec:
  syncPolicy:
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=true
  destination:
    namespace: {{ .Values.openstack_mariadb.namespace }}
    server: https://192.168.56.151:6443
  project: default
  source:
    chart: mariadb
    repoURL: https://tarballs.opendev.org/openstack/openstack-helm
    targetRevision: {{ .Values.openstack_mariadb.version }}
    helm:
      valueFiles:
        - values.yaml
      values: |-
        {{- toYaml .Values.openstack_mariadb.values | nindent 8 }}
